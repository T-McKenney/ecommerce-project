import { FireSaleShopValidators } from './fire-sale-shop-validators';

describe('FireSaleShopValidators', () => {
  it('should create an instance', () => {
    expect(new FireSaleShopValidators()).toBeTruthy();
  });
});
